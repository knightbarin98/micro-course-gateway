spring:
  application:
    name: micro-course-gateway
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      default-zone: http://localhost:8761/eureka
server:
  port: 8700

